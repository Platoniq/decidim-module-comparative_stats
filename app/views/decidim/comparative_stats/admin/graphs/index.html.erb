<div class="card" id="translation_sets">
  <div class="card-divider">
    <h2 class="card-title flex--sbc">
      <div>
        <%= t ".title" %>
      </div>
    </h2>
  </div>
</div>

    <% if active_endpoints.any? %>

      <ul class="tabs" data-tabs id="charts-tabs">
        <li class="tabs-title is-active"><a href="#panel1" aria-selected="true">Global stats</a></li>
        <li class="tabs-title"><a href="#panel2">Processes timeline</a></li>
      </ul>

      <div class="tabs-content" data-tabs-content="charts-tabs">
        <div class="tabs-panel is-active" id="panel1">
          <%= cell("decidim/comparative_stats/metric_piecharts", global_metrics_graph) %>
        </div>
        <div class="tabs-panel" id="panel2">
          <%= cell("decidim/comparative_stats/participatory_processes_timeline", timeline_graph) %>
        </div>
      </div>

    <% else %>
    <%= t ".no_endpoints" %>
    <% end %>
